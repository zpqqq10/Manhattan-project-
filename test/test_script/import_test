import Students from ./test/test_data/add.csv (id int,name char(10) unique,gender char(1),primary key(id));
import AAA from ./test/test_data/add.csv (id int,name char(10) unique,gender char(1),primary key(id));

select * from aaa where id < 15;
select * from aaa where id>300 and id < 350 and gender = 'f';
drop table aaa;